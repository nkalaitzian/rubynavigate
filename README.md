# RubyNavigate

Jump to Ruby classes and modules by name.

## Usage

- Run the command: [RubyNavigate] Find Class/Module
- Type a class or module name (e.g. `Foo::Bar`)
- Select a result to open the file
